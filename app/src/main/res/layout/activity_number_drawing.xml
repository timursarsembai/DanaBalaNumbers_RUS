<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/child_gradient_yellow"
    android:padding="16dp"
    tools:context=".NumberDrawingActivity">

    <!-- Верхняя панель: назад + заголовок -->
    <LinearLayout
        android:id="@+id/topBar"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginBottom="12dp">

        <ImageView
            android:id="@+id/backButton"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:src="@drawable/ic_arrow_back"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:padding="4dp"
            android:clickable="true"
            android:focusable="true"
            android:contentDescription="Назад" />

        <TextView
            android:id="@+id/screenTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="🎨 Рисование цифр"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="#1565C0"
            android:layout_marginStart="12dp"
            android:gravity="start" />
    </LinearLayout>

    <!-- Тонкий прогресс-бар под шапкой -->
    <ProgressBar
        android:id="@+id/progressBar"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="0dp"
        android:layout_height="8dp"
        app:layout_constraintTop_toBottomOf="@id/topBar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:max="100"
        android:progress="0"
        android:layout_marginBottom="12dp"
        android:progressTint="@color/successColor"
        android:progressBackgroundTint="#E0E0E0" />

    <!-- Маленький индикатор текущей цифры и подсказки (под шапкой, над канвасом) -->
    <LinearLayout
        android:id="@+id/miniHeader"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        app:layout_constraintTop_toBottomOf="@id/progressBar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginBottom="8dp">

        <TextView
            android:id="@+id/numberDisplay"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:text="0"
            android:textSize="24sp"
            android:textStyle="bold"
            android:textColor="#1565C0"
            android:gravity="center"
            android:background="@drawable/circle_background"
            android:layout_marginEnd="12dp" />

        <TextView
            android:id="@+id/instructionText"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Закрась цифру 0"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="#2C3E50"
            android:gravity="start" />
    </LinearLayout>

    <!-- Область рисования -->
    <com.timursarsembayev.danabalanumbers.DrawingView
        android:id="@+id/drawingView"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_margin="12dp"
        android:background="@drawable/drawing_area_background"
        app:layout_constraintTop_toBottomOf="@id/miniHeader"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@id/colorPalette" />

    <!-- Палитра цветов -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/colorPalette"
        android:layout_width="0dp"
        android:layout_height="80dp"
        android:padding="8dp"
        android:clipToPadding="false"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@id/buttonLayout" />

    <!-- Кнопки управления -->
    <LinearLayout
        android:id="@+id/buttonLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="8dp"
        android:gravity="center"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Ластик с кольцом-выделением как у палитры цветов -->
        <FrameLayout
            android:id="@+id/eraserContainer"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:layout_marginEnd="12dp">

            <ImageButton
                android:id="@+id/eraserButton"
                android:layout_width="56dp"
                android:layout_height="56dp"
                android:layout_gravity="center"
                android:background="@drawable/eraser_button_bg"
                android:src="@drawable/ic_eraser"
                android:scaleType="centerInside"
                android:tint="@android:color/white"
                android:contentDescription="Ластик" />

            <ImageView
                android:id="@+id/eraserSelectionRing"
                android:layout_width="56dp"
                android:layout_height="56dp"
                android:layout_gravity="center"
                android:src="@drawable/color_selection_ring"
                android:visibility="gone" />
        </FrameLayout>

        <!-- Кнопка «Готово» / «Далее» -->
        <Button
            android:id="@+id/doneButton"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:text="Готово"
            android:textSize="14sp"
            android:background="@drawable/button_primary"
            android:textColor="@android:color/white" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
